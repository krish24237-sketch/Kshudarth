DROP YOUR WORK VIDEOS IN THIS FOLDER
====================================

This folder is served at the URL /work/ on the site.
A file here called  ai-call-agent.mp4  is reachable at  /work/ai-call-agent.mp4

Use these exact filenames — the site references them in lib/content.ts:

  ai-call-agent.mp4      (SaaS animation — already uploaded)
  saas-demo.mp4          (SaaS animation — already uploaded)
  pehle-anda.mp4         (Balraj — "Pehle Anda Ya Murgi", 2.1K views)
  i-quit-everything.mp4  (growwithprabin — "I Quit Everything")
  chuze-ki-diary.mp4     (Balraj — "Chuze Ki Diary" series)

RULES
-----
- All lowercase, hyphens instead of spaces.
- No spaces, no brackets, no parentheses in filenames — they break URLs.
- .mp4 (H.264) is the safe format; it plays everywhere.
- Keep each file under ~10MB if you can, or the page gets slow.

To add more later: drop the file here, then add an entry to the
workSamples array in lib/content.ts.
